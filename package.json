{
  "name": "svg-optimizer",
  "version": "1.1.0",
  "description": "SVG Optimizer - Fast, stateless SVG compression tool",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "cd client && npm install && npm run build",
    "vercel-build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "multer": "^1.4.5-lts.1",
    "razorpay": "^2.9.2",
    "svgo": "^3.3.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
